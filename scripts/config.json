{
  "search": {
    "query": "${SEARCH_QUERY}",
    "max_results": 100,
    "days_back": 3
  },
  "filter": {
    "keywords": "${KEYWORDS}"
  },
  "slack": {
    "webhook_url": "${SLACK_WEBHOOK_URL}"
  },
  "scheduling": {
    "enabled": true,
    "time": "08:00",
    "timezone": "UTC"
  }
}
